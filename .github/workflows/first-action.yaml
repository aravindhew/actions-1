name: My First WorkFlow

on: push 

jobs:
    first_job:
        runs-on: ubuntu-latest
        steps:
            - name: checkout
              uses: actions/checkout@v4
  
            - name: welcome message
              run: echo "my first github actions from runner"

            - name: list filessfdsdf
              run: ls

            - name: reading a file
              run: cat README.md
